# ARMarketer 開發紀錄 - 2026-05-13

## 本次會話進度摘要
今天我們完成了從「純 Viewer」到「高效能 AR Marketing Platform」的戰略轉型，並啟動了 Phase 1 的前兩週任務。

### 1. 戰略與規劃更新
- **專案命名**：正式更名為 **ARMarketer**。
- **核心目標**：解決 Zapworks 25MB 限制，支援 100MB+ 模型與 Image Tracking AR。
- **文件重構**：
    - 更新 `system_plan.md` 作為總綱。
    - 建立 `docs/plan/` 目錄，包含 `overview`, `architecture`, `phases`, `strategy`, `validation`, `roi` 等模組化文件。
    - 制定了為期 10 週的 **風險優先 (Fail-fast) 開發計劃**。

### 2. 技術實作 (Task A: Viewer POC)
- **MindAR 驗證 (Gate 1)**：
    - 建立了 `public/poc.html`，透過 CDN 載入 MindAR。
    - 驗證了 Web-based Image Tracking 在手機瀏覽器的可行性。
- **大型素材 Viewer (Gate 2)**：
    - 實作了 `src/components/ModelViewer.tsx`。
    - **進度回饋**：具備即時百分比進度條，優化大檔案載入體驗。
    - **資源釋放**：實作了針對大型模型 (100MB+) 的 `dispose` 邏輯，防止行動端記憶體崩潰。
    - **多格式支援**：同時支援 OBJ 與 FBX。

### 3. 目前狀態
- **前端工作流**：已完成 Viewer 骨架，準備進行 100MB 壓力測試。
- **後端工作流**：待進行 Prisma Schema 起稿 (由使用者接手)。
- **部署環境**：已完成 Vercel 部署準備。

## 下一步計劃 (W2-W3)
- [ ] 執行 100MB OBJ 實機載入測試 (Gate 2)。
- [ ] 啟動 W3 任務：Blender Docker Pipeline 轉檔實驗。
- [ ] 整合後端 Schema。
